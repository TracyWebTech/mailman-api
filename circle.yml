
machine:
  services:
    - docker

dependencies:
  override:

test:
  override:
    - tests/run.sh
  post:
    - docker run -e "COVERALLS_REPO_TOKEN=$COVERALLS_REPO_TOKEN" -w /srv/mailman-api -v $DIR/../:/srv/mailman-api tracywebtech/mailman-api
